{
  "compileOnSave": true,
  // must explicitly not transpile files on its own,
  // or it will recurse down through subfolders trying
  // to compile it with this config - which will fail
  "files": [],
  "references": [
    { "path": "./main" },
    { "path": "./messages"},
    { "path": "./worker" }
  ]
}